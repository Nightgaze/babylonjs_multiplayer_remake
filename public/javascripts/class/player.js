
function Player(game)
{
    var engine = game.getEngine();

    var admin = true;
    var name;
    var socket = new Socket(game);
    

    if (admin) 
    {
        //GUI:
        var modelSelector = new ModelSelector(game, socket);
        
         

    }
}





